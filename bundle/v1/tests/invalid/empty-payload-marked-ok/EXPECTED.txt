应报错：载荷零长度却记为 verdict=ok（SPEC §6.5.2）。
（这份夹具是把 content_sha256 改成空串的哈希、正文原样留着造出来的，
 所以现在还会额外报一条 content_sha256 不符——同一句谎被两条检查
 分别看见，不是两个毛病。）
真实旧档案里出现过 7 个零字节文件，与一次会话失效同批产生，文件名齐全地躺在目录里，磁盘上没有任何东西表明抓取失败过——下游只会看到「文件在」。空响应必须如实判定。
